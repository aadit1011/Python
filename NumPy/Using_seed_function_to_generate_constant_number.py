import numpy as np  # Import the NumPy library for numerical operations

# Set the seed for the random number generator to ensure reproducibility
np.random.seed(1)

# Generate an array 'v' of 31 random integers between 10 (inclusive) and 20 (exclusive)
v = np.random.randint(10, 20, 31)

# Print the generated array 'v'
v
